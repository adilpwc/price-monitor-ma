[project]
name = "price-monitor-ma"
version = "0.1.0"
description = "Moroccan price monitoring system"
requires-python = ">=3.12"
dependencies = ["httpx","beautifulsoup4","lxml","rapidfuzz","pyyaml"]

[project.optional-dependencies]
dev=["pytest","pytest-cov","ruff","mypy"]
